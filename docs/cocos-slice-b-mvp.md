# 第二轮验证 · 最小循环（不是正式开工）

> 历史过线记录。`MvpLoop` 已删，正式入口是 `MainController`。日常巡环看 [framework-loop.md](framework-loop.md)。

2026-08-17。Hans 授权：用 MCP 跑通最小 MVP，证明「选车 → 一段三车道 → 本地结算」在 Cocos 里能转。**不是**第二刀正式方案，不出图、不接广告/好友榜。

## 过线

在 Creator 预览里能：

1. 看到选车页（仅跑车可开）
2. 发车后三车道上有对向色块、路程数字在涨
3. 到终点或撞没后出现四项本地结算

已在编辑器 Game View 看到这三屏。控制台无脚本报错。

## 在哪

- 工程：`midnightroad`，场景仍是 `assets/scene.scene`
- 脚本：`assets/scripts/MvpLoop.ts`（单文件状态机）
- 色块占位，默认精灵框，不是美术资产

## 这一刀故意没有的

机位 A 透视、像素城、五车都能开、碾压、激励广告、好友榜、微信包重编、正式任务拆解。

## 怎么自己点

Creator 打开 `midnightroad`，预览当前场景。发车；左右滑换道；按住右下「氮」。撞两下或开完短段进结算。

微信包要另点「发布到微信小游戏」。MCP `editor.build` 不能代替那一步。
