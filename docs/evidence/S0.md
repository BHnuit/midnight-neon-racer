# S0 工程基线证据

S0 不改产品行为。Cocos base / result：`main` / `dda5a64af9bd8f78266165beca7eae34de6bd159`。正式实现分支：`BHnuit/s1-formal-second-slice`（同 SHA，尚未改玩法）。

## §5.6 架构核对

- Result: PASS
- Timestamp: 2026-08-18 01:23 Asia/Shanghai
- Read: `game-architect` 的 `macro-design.md`、`principles.md`、`project-structure.md`、`system-time.md`
- Decision:
  - 维持方案 §6 的单向依赖：`types/GameBalance -> RoadFactory -> RunSession -> GameDirector -> Cocos`，唯一外部 seam 是 `PlatformPort`
  - `RunSession` 用固定时间步；`GameDirector` 用枚举 FSM，不引入状态类、行为树、命令队列或模块管理框架
  - S0 只建测试工具，不预建空 `core/app/cocos/platform` 文件
- Why no new abstraction: 第一版是单机微信客户端，没有需要隔离的第二套运行时；微信 SDK 才是真实外部依赖
- Limits: 调用本身不是 S0 通过证据

## S0-CORE-01 · 测试与独立 typecheck 空跑

- Result: PASS
- Timestamp: 2026-08-18 01:22 Asia/Shanghai
- Contract commit: `36df70e4214152ac9d057e673edcea1fb7123e52`
- Cocos branch / base / result: `BHnuit/s1-formal-second-slice` / `dda5a64` / `dda5a64`
- Environment: Node v24.14.0；vitest 2.1.9；typescript 5.x；不依赖 Creator `temp/tsconfig.cocos.json`
- Preconditions: `midnightroad` 根执行 `npm ci` 等价安装
- Steps:
  1. `npm test`
  2. `npm run typecheck:core`
  3. `rg -n "from ['\"]cc['\"]|\\bwx\\." assets/scripts/core`
- Expected: 测试通过；typecheck 无输出；core 目录无 `cc`/`wx` 命中
- Actual: 2 tests passed；`tsc -p tsconfig.core.json --noEmit` 退出 0；core 源码尚未创建，扫描无命中
- Artifact: `tests/core/harness.test.ts`
- Limits: 只证明夹具可跑，不是玩法行为测试

## S0-CREATOR-01 · 打开色块场景并校验引用

- Result: PASS
- Timestamp: 2026-08-18 01:22 Asia/Shanghai
- Cocos branch / base / result: `BHnuit/s1-formal-second-slice` / `dda5a64` / `dda5a64`
- Environment: Cocos Creator 3.8.6 arm64 darwin；Cocos MCP 1.7.9；画布 720×1280
- Preconditions: Creator 已打开 `midnightroad`
- Steps:
  1. `cocos_editor project_info` / `editor_info`
  2. `cocos_scene open db://assets/scene.scene`
  3. `cocos_scene validate_scene`
  4. `cocos_validate references`
  5. `cocos_editor console_logs logType=error`
  6. `cocos_capture screenshot mode=content 720×1280`
- Expected: 工程路径正确；场景可开；无断裂引用；无 error 日志
- Actual: 工程 `/Users/hant/工作台/projects/creation/midnightroad`，设计分辨率 720×1280；场景打开；`brokenCount=0`；error 日志 0 条；截到选车屏
- Artifact: [s0/creator-scene-720x1280.png](s0/creator-scene-720x1280.png) sha256 `4c13eef98eb9b48d726200c7a107db3814a5802dd27c7664518b03c7126e8b04`
- Limits: 截图是 Creator 色块验证屏，不是微信包画面

## S0-TEMPLATE-01 · wechatgame 模板与 3.8.6 默认 diff

- Result: PASS
- Timestamp: 2026-08-18 01:20 Asia/Shanghai
- Default: `/Applications/Cocos/Creator/3.8.6/CocosCreator.app/Contents/Resources/resources/3d/engine/templates/wechatgame/`
- Actual: `game.ejs`、`game.json`、`project.config.json` 与默认完全一致。方向/DPR 段落属于引擎默认模板，不是项目私改，予以保留
- Limits: 未做 Android/iOS 真机触摸坐标对照；R2 仍开放到有真机证据

## S0-GIT-01 · 双仓状态与 Cocos 基线

- Result: PASS
- Timestamp: 2026-08-18 01:24 Asia/Shanghai
- Contract: worktree `BHnuit/第一轮开发`，result `36df70e`
- Cocos: 先前无 commit；现 `main` = `dda5a64`；实现分支 `BHnuit/s1-formal-second-slice`
- Ignored and not committed: `library/`、`temp/`、`local/`、`build/`、`extensions/`、`.creator/`、`.grok/`、MCP 端口/私钥、`.DS_Store`
- Limits: Cocos 仓尚无 remote；契约仓未跟踪根 `package-lock.json`（Netlify 遗留，与 S0 无关）

## S0-PKG-01 · 微信空包助手 run_game / get_logs

- Result: BLOCKED
- Timestamp: 2026-08-18 01:22 Asia/Shanghai
- Environment: 当前 Grok 会话未暴露 `weixin-minigame-helper` 工具
- Preconditions: 导出包存在 `/Users/hant/工作台/projects/creation/midnightroad/build/wechatgame/game.js`（2026-08-17 第一刀）
- Steps attempted:
  1. 直接调用助手 MCP：工具不存在
  2. `check-mcp.mjs --json`：`installed` 随后由 `install-mcp.mjs` 装到 `~/.npm-global/lib/node_modules/@weadmin/weixin-minigame-helper-mcp`；`recommendedAction=ask-user`（无 host 强信号）
  3. 按 Grok 文档把 stdio 服务写入用户级 `~/.grok/config.toml` 的 `[mcp_servers.weixin-minigame-helper]`，**未**写入契约仓 `.mcp.json`
- Expected: `run_game(workspacePath=build/wechatgame)` → 打开预览 → 约 2 秒后 `get_logs` 无 error，并留一张截图
- Actual: 本会话 MCP 工具表仍无助手入口，按 R10 标 BLOCKED，不用 shell 假冒
- Artifact: none
- Limits: 需要新会话热加载用户级 MCP 后再补 `S0-PKG-02`。这只证明空包能启动，不把第一刀升级成正式玩法证据
