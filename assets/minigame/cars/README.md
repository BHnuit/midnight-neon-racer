# 车辆素材

每辆车使用独立子目录，保存透明底大师图、运行时尺寸候选、预览和 `manifest.md`。

当前只有 `paoche/` 作为跑车锚点。其余小货车、摩托车、飞行汽车和压路机必须先按 `../../../CONTEXT.md` 的能力定位完成造型验收，再新增目录；不要用一套换色图冒充五种玩法身份。

车辆精灵必须与赛道底板分离，并在导入 Cocos 前验证车道宽度、接地点、碰撞轮廓和透明边缘。
